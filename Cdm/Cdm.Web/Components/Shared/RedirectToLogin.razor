@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        var currentUri = Uri.EscapeDataString(Nav.Uri);
        Nav.NavigateTo($"/login?returnUrl={currentUri}", forceLoad: false);
    }
}
