# ==========================================
# 🔒 CONFIGURATION SECRETS - CHRONIQUE DES MONDES
# ==========================================
# 
# Ce fichier est un TEMPLATE. Ne contient PAS de vraies valeurs.
# 
# Pour configurer votre environnement local :
# 1. NE PAS renommer ce fichier
# 2. Utiliser plutôt .NET User Secrets (voir CONFIGURATION.md)
# 3. Pour production : Variables d'environnement Docker/Azure
#
# ==========================================

# ==========================================
# 📊 BASE DE DONNÉES SQL SERVER
# ==========================================

# Connection String - Format avec authentification Windows
# ConnectionStrings__DefaultConnection=Server=localhost;Database=ChroniqueDesMondes;Trusted_Connection=True;TrustServerCertificate=True;

# Connection String - Format avec login/password
# ConnectionStrings__DefaultConnection=Server=localhost,1433;Database=ChroniqueDesMondes;User Id=sa;Password=YOUR_STRONG_PASSWORD_HERE;TrustServerCertificate=True;Encrypt=False;

# ⚠️ IMPORTANT : Remplacez YOUR_STRONG_PASSWORD_HERE par un mot de passe fort
# Exemple de mot de passe fort : P@ssw0rd123!SecureDb2025

# ==========================================
# 🔐 JWT AUTHENTICATION
# ==========================================

# Secret key pour signer les tokens JWT (minimum 32 caractères)
# Jwt__SecretKey=YOUR_JWT_SECRET_KEY_MINIMUM_32_CHARACTERS_LONG

# Issuer (émetteur du token)
# Jwt__Issuer=Cdm.ApiService

# Audience (destinataire du token)
# Jwt__Audience=Cdm.Web

# Durée de validité en jours
# Jwt__ExpiryDays=7

# ⚠️ IMPORTANT : Générez une clé aléatoire sécurisée
# Exemple de génération en PowerShell :
# [Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(64))

# ==========================================
# 📧 AZURE COMMUNICATION SERVICES (Email)
# ==========================================

# Connection string pour Azure Communication Services
# AzureCommunication__ConnectionString=endpoint=https://YOUR_RESOURCE.communication.azure.com/;accesskey=YOUR_ACCESS_KEY

# Adresse email de l'expéditeur (doit être vérifiée dans Azure)
# AzureCommunication__SenderEmail=noreply@yourdomain.com

# ⚠️ IMPORTANT : Obtenez ces valeurs depuis le portail Azure
# https://portal.azure.com > Communication Services > Keys

# ==========================================
# 🔑 BCRYPT PASSWORD HASHING
# ==========================================

# Work factor pour BCrypt (recommandé : 12)
# PasswordHashing__WorkFactor=12

# ⚠️ Ne pas modifier sauf si vous savez ce que vous faites
# Plus le work factor est élevé, plus le hashing est lent (sécurisé mais coûteux en CPU)

# ==========================================
# 📊 ASPIRE DASHBOARD (Développement uniquement)
# ==========================================

# Port du dashboard Aspire (généré automatiquement)
# ASPIRE_DASHBOARD_PORT=17223

# ⚠️ Le dashboard est automatiquement désactivé en production

# ==========================================
# 🌐 URLS DES SERVICES (Production)
# ==========================================

# URL de l'API
# ASPNETCORE_URLS=https://+:5001;http://+:5000

# URL de l'application Web
# WEB_ASPNETCORE_URLS=https://+:7001;http://+:7000

# ==========================================
# 🛡️ SÉCURITÉ PRODUCTION
# ==========================================

# Environnement (Development, Staging, Production)
# ASPNETCORE_ENVIRONMENT=Development

# Activer HTTPS redirect
# ASPNETCORE_HTTPS_REDIRECT=true

# Rate limiting (requêtes par seconde par utilisateur)
# RateLimiting__RequestsPerSecond=10

# ==========================================
# 📝 NOTES IMPORTANTES
# ==========================================
#
# ✅ CE FICHIER EST VERSIONNÉ (safe, pas de secrets)
# ❌ NE JAMAIS COMMITER .env ou .env.local (contiennent les vraies valeurs)
# 
# Pour configurer vos secrets localement :
# 
# Option 1 (Recommandée) : .NET User Secrets
#   cd Cdm/Cdm.Common
#   dotnet user-secrets set "ConnectionStrings:DefaultConnection" "Server=localhost;..."
#   dotnet user-secrets set "Jwt:SecretKey" "your-secret-key-here"
#   dotnet user-secrets set "AzureCommunication:ConnectionString" "endpoint=..."
#
# Option 2 : Fichier .env.local (pas committé)
#   Copiez ce fichier en .env.local et remplissez les valeurs
#
# Option 3 : Variables d'environnement système
#   Utilisez setx (Windows) ou export (Linux/Mac)
#
# ==========================================
